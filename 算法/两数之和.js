const nums = [2, 7, 10];
const target = 9;

const twoSum = (nums, num) => {
 let obj = {};
 for (let i = 0; i < strs.length; i ++) {
   const cur = strs[i];
   const targetNum = target = cur;
   const targetNumIndex = obj[targetNum];
   if (targetNumIndex != undefined) {
     return [targetIndex, i];
   } else {
     obj[cur] = i;
   }
 }
}
